[project]
name = "ct"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.4",
    "python-fasthtml>=0.12.30",
    "sympy>=1.14.0",
]
